<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZBX">

        <!-- Добавь это свойство вместо атрибута в application -->
        <property
            android:name="android.enableOnBackInvokedCallback"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ZBX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".widget.ProblemWidgetService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <!-- Провайдер виджета - ОДИН объединенный receiver -->
        <receiver
            android:name=".widget.ProblemWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.itsoul.zbxclient.ACTION_REFRESH_WIDGET" />
                <action android:name="com.itsoul.zbxclient.ACTION_TOGGLE_ACK" />
                <action android:name="com.itsoul.zbxclient.ACTION_TOGGLE_MAINT" />
                <action android:name="com.itsoul.zbxclient.ACTION_FORCE_REFRESH" />
                <action android:name="com.itsoul.zbxclient.LANGUAGE_CHANGED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/problem_widget_info" />
        </receiver>

        <!-- Активность для конфигурации виджета -->
        <activity
            android:name=".widget.ProblemWidgetConfigureActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Сервис для удаленного списка в виджета -->
        <service
            android:name=".widget.ProblemWidgetRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <receiver
            android:name=".widget.ThemeChangeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.itsoul.zbxclient.THEME_CHANGED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>